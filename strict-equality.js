// " = " this is the assignment operator
// " == " this is the comparision operator (checks whether some values are equal)
// " != " this is the ineguality operator
// " !== " this the strict inequality operator
// " === " this is the strict equality operator

const PI = 3.14;

if (PI === "3.14") {
  console.log("thats not PI");
} else {
  console.log("thats PI");
}
