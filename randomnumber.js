// generating random numbers

const max = 100; // setting limits to ne reached by the generator function
const min = 20;

let randomNumber = Math.floor(Math.random() * max) + min;

console.log(randomNumber);

// useful for games like dice rolling
